package com.example.blogapp.utils;
//this class is used for keeping app constants so that they can be called in used in diferent parts without using duplicates and having to repeat yourself all the time
public class AppConstants {
    //when creating a constant all the letters in the name should be in capital and multiple words separated by underscore
    public static final String DEFAULT_PAGE_NUMBER = "0";
    public static final String DEFAULT_PAGE_SIZE = "10";
    public static final String DEFAULT_SORTBY = "id";
    public static final String DEFAULT_SORTDIRECTION = "asc";
}
